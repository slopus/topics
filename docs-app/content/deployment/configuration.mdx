---
title: Configuration
description: The complete reference for every STREAMS_* environment variable — networking and auth, storage and tiering, resource limits — with exact defaults and behavior.
---

import { Callout } from 'nextra/components'

# Configuration

streams is configured entirely from the **environment** — there is no config file. This
page is the complete reference: every `STREAMS_*` variable plus `RUST_LOG`, with its exact
default and what it controls. Unset variables fall back to the defaults shown here.

<Callout type="info">
  Every `STREAMS_MAX_*` limit accepts `0` to mean **unlimited**. Exceeding a configured cap
  returns **429 `throttled`** with the offending bound in `error.detail.limit`. See
  [Resource limits](#resource-limits) below and [Errors](/api/errors).
</Callout>

## Networking & auth

Where the server binds, whether auth is on, and which keys are accepted. The default bind
is **loopback**, so an unconfigured server is never accidentally public.

| Env var | Default | Meaning |
|---|---|---|
| `STREAMS_HOST` | `127.0.0.1` | Bind host (loopback by default). May also be a full `host:port` — in which case it carries the port and `STREAMS_PORT` is ignored. |
| `STREAMS_PORT` | `4000` | Listen port. Used unless `STREAMS_HOST` is a full address. |
| `STREAMS_API_KEYS` | _(unset)_ | Comma-separated bearer keys, hashed SHA-256 at rest. Each entry is `key` \| `key:scopes` \| `key:scopes:prefixes` \| `key::prefixes`. Unset ⇒ **auth disabled** (dev mode). |
| `STREAMS_ALLOW_INSECURE_NO_AUTH` | `0` | Required to start on a **non-loopback** bind with **no** keys — otherwise the server refuses to start. Local/dev only. |
| `STREAMS_PROBE_AUTH` | `false` | When `true`, require auth on the `/v0/health`, `/v0/ready`, and `/v0/metrics` probes too. By default probes skip auth. |

<Callout type="warning">
  On a non-loopback bind with no `STREAMS_API_KEYS`, the server **refuses to start** unless
  `STREAMS_ALLOW_INSECURE_NO_AUTH=1` — it would otherwise be an open, unauthenticated event
  store. Never set that flag on a network-exposed deployment. The key entry syntax (scopes
  and the box-name prefix allowlist) is documented in full on the
  [Security](/deployment/security) page.
</Callout>

A bare `key` grants full access (back-compat). The `scopes` field draws from
`read`/`write`/`delete`/`admin` (single-letter aliases `r`/`w`/`d`/`a`, plus `rw`); an empty
scopes field means all scopes. The `prefixes` field is a box-name prefix allowlist. A
**malformed scope token makes the server refuse to start** (fail-closed). See
[Security](/deployment/security) for the per-route scope requirements and how the allowlist
is enforced on paths, request bodies, and list results.

## Storage & tiering

Where durable state lives, when segments are sealed, and the optional cold tier.
`STREAMS_DATA_DIR` is the hot-tier root; the segment-sealing knobs control segment
rotation; the hot-retention knobs govern when sealed segments relocate to cold storage.

| Env var | Default | Meaning |
|---|---|---|
| `STREAMS_DATA_DIR` | `./streams-data` | Root for the WAL, segments, and snapshots (the hot tier). Replayed on start; a missing/empty dir is a fresh start. |
| `STREAMS_WAL_SHARDS` | `min(num_cpus, 8)` | Number of independent WAL shards (each its own writer thread, file set, and group-commit loop). Each box maps to one shard by a stable hash of its id, so per-box ordering + durability still hold; recovery is **shard-count-agnostic** (replays all shards by `box_id`), so this may change between restarts. `1` = the flat single-writer layout. |
| `STREAMS_COLD_DIR` | _(unset)_ | Cold-tier directory. Set ⇒ sealed segments past the hot-retention bound relocate here off the hot path. Unset ⇒ tiering disabled (everything stays hot). |
| `STREAMS_SEGMENT_MAX_EVENTS` | `10000` | Seal the active segment after this many records. |
| `STREAMS_SEGMENT_MAX_BYTES` | `67108864` (64 MiB) | Seal after this many `.data` bytes. |
| `STREAMS_SEGMENT_MAX_AGE_MS` | `3600000` (1 h) | Seal an idle/partial segment after this age. `0` disables age-based sealing. |
| `STREAMS_HOT_RETAIN_SEGMENTS` | `4` | Number of newest sealed segments kept hot before relocating older ones to cold. |
| `STREAMS_HOT_RETAIN_BYTES` | `0` (off) | Optional hot sealed-byte bound. When both this and `STREAMS_HOT_RETAIN_SEGMENTS` are set, the **stricter** of the two wins. |

<Callout type="info">
  Tiering is opt-in: with `STREAMS_COLD_DIR` unset, every segment stays in the hot tier and
  the retention knobs have no effect. Cold reads run on a separate I/O pool and **never block
  writes or live SSE delivery**. See [Storage & Tiering](/deployment/storage-and-tiering).
</Callout>

## Routers

| Env var | Default | Meaning |
|---|---|---|
| `STREAMS_FORWARD_V2` | `0` | Selects the **async + derived** router-forwarding path (durable per-router cursor, forwarded copies not WAL-logged so one source append is one WAL write, single-source-per-dest, replay-from-cursor recovery) described in [Routers](/api/routers). Default `0` keeps the legacy synchronous in-line forward; set `1` to enable the cursor-driven async path. |

## Resource limits

DoS-hardening caps. Each is a guardrail with a sane default; **`0` means unlimited**, and
crossing a cap returns **429 `throttled`** carrying the bound in `error.detail.limit`.

| Env var | Default | Meaning |
|---|---|---|
| `STREAMS_MAX_BODY_BYTES` | `67108864` (64 MiB) | Maximum request body size. |
| `STREAMS_MAX_BOXES` | `100000` | Maximum number of boxes. |
| `STREAMS_MAX_ROUTERS` | `10000` | Maximum number of routers. |
| `STREAMS_MAX_WATCH_SESSIONS` | `10000` | Maximum live watch sessions. |
| `STREAMS_MAX_SSE_CONNECTIONS` | `10000` | Maximum concurrent SSE connections, server-wide. |
| `STREAMS_MAX_SSE_CONNECTIONS_PER_KEY` | `1000` | Maximum concurrent SSE connections per key. |
| `STREAMS_MAX_INFLIGHT_PER_KEY` | `1000` | Maximum concurrent in-flight requests per key. |
| `STREAMS_MAX_TOTAL_BYTES` | `0` (unlimited) | Global quota on total retained record bytes across all boxes. A write past it ⇒ `429 throttled`. |

<Callout type="info">
  An over-cap request returns `429 throttled`, distinct from the *elastic* throttling the
  scheduler applies under CPU pressure (which adds `detail.retry_after_ms` instead). A cap
  rejection is a hard limit; elastic throttling degrades latency, never correctness. See
  [Errors](/api/errors).
</Callout>

## Logging

| Env var | Default | Meaning |
|---|---|---|
| `RUST_LOG` | `info` | Tracing filter. Standard Rust `tracing` syntax (e.g. `RUST_LOG=streams=debug,info`) — not streams-specific. |

## A worked example

A production-style invocation that binds a non-loopback address, turns auth on, points at a
dedicated data directory with a cold tier, and tightens a couple of limits:

```bash
STREAMS_HOST=0.0.0.0 \
STREAMS_PORT=4000 \
STREAMS_API_KEYS="prod-key:rw:tenant42:,admin-key:a" \
STREAMS_DATA_DIR=/var/lib/streams \
STREAMS_COLD_DIR=/mnt/cold/streams \
STREAMS_MAX_BOXES=50000 \
STREAMS_MAX_TOTAL_BYTES=10737418240 \
./target/release/streams
```

Run this behind a TLS-terminating reverse proxy — streams speaks plain HTTP. The same
variables apply to the container image; see [Docker](/deployment/docker).

## See also

- [Security](/deployment/security) — the key entry syntax, scopes, the prefix allowlist, and the resource model.
- [Storage & Tiering](/deployment/storage-and-tiering) — the data directory, segment sealing, and hot→cold relocation.
- [Running streams](/deployment) — start, recovery, binding, and graceful shutdown.
- [Docker](/deployment/docker) — passing these variables to the published image.
